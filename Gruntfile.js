
'use strict';

module.exports = function ( grunt ) {

    // Configure and register tasks
    grunt.loadTasks ( 'grunt/tasks/configuration'   );
    grunt.loadTasks ( 'grunt/tasks/register'        );

};
